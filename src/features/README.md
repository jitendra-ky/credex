/**
 * @fileoverview Feature modules
 * Each feature should have its own subdirectory containing:
 * - Components specific to that feature
 * - Hooks specific to that feature
 * - Types/interfaces
 * - Services/API calls
 * - Utils
 *
 * Example structure:
 * features/
 *   ├── auth/
 *   │   ├── components/
 *   │   ├── hooks/
 *   │   ├── services/
 *   │   ├── types/
 *   │   └── utils/
 *   ├── dashboard/
 *   └── settings/
 */

export {};
